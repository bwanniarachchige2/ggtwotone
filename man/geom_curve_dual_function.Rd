% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom_curve_dual_function.R
\name{geom_curve_dual_function}
\alias{geom_curve_dual_function}
\title{Dual-Tone Curved Function Lines}
\usage{
geom_curve_dual_function(
  fun,
  xlim = c(-3, 3),
  n = 701,
  curvature = 0,
  angle = 90,
  ncp = 5,
  colour1 = NULL,
  colour2 = NULL,
  base_color = NULL,
  contrast = 4.5,
  method_contrast = "WCAG",
  linewidth = 1.2,
  args = list(),
  smooth = TRUE,
  color1 = NULL,
  color2 = NULL,
  alpha = 1,
  ...
)
}
\arguments{
\item{fun}{A function to evaluate (e.g., \code{dnorm}, \code{dt}).}

\item{xlim}{Range of x-values to evaluate over (numeric vector of length 2).}

\item{n}{Number of segments to compute (default: 201).}

\item{curvature, angle, ncp}{Passed to underlying \code{geom_curve_dual} segments.}

\item{colour1, colour2}{Fixed top/bottom stroke colours. If only colour1 given,
colour2 is derived for contrast. (Aliases color1/color2 also accepted.)}

\item{base_color}{Optional base color to derive a contrast pair (overrides colour1/colour2 if supplied).}

\item{contrast, method_contrast}{Passed to adjust_contrast_pair() when deriving colors.}

\item{linewidth}{Stroke width for the top line.}

\item{args}{List of arguments passed to \code{fun} (for example, list(df = 1) for \code{dt}).}

\item{smooth}{Use smooth dual-stroke curves (\code{geom_path}) instead of segmented curves (\code{geom_curve_dual}). Default is TRUE.}

\item{color1, color2}{U.S.-spelling aliases for \code{colour1}/\code{colour2}. Identical
in effect; prefer \code{colour1}/\code{colour2} in code examples.}

\item{alpha}{Overall opacity for both strokes (0–1).}

\item{...}{Additional arguments passed to \code{geom_curve_dual()}.}
}
\value{
A \code{ggplot2} layer with curved segments.
}
\description{
Draws a function (e.g., density or mathematical curve) using perceptually offset dual-stroke curved line segments.
}
\examples{
library(ggplot2)

base <- ggplot() + xlim(-2.05,2.05)
base +
  geom_curve_dual_function(
  fun = function(x) 0.5 * exp(-abs(x)),
  xlim = c(-2, 2),
  color1 = "#EEEEEE",
  color2 = "#222222",
  linewidth = 1,
  smooth = TRUE
  ) +
  theme_dark()


ggplot() +
  geom_curve_dual_function(
    fun = dnorm,
    xlim = c(-5, 5),
    base_color = "green",
    linewidth = 1,
    smooth = TRUE
  ) +
  geom_curve_dual_function(
    fun = dt,
    args = list(df = 1),
    xlim = c(-5, 5),
    base_color = "brown",
    linewidth = 1,
    smooth = TRUE
  ) +
  theme_dark()

}
