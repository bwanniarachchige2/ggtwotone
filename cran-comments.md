## Test environments

* local macOS (R 4.5.2)

### GitHub Actions

* ubuntu-latest (release)
* ubuntu-latest (oldrel-1)
* ubuntu-latest (devel)
* macos-latest (release)
* windows-latest (release)

## R CMD check results

0 errors | 0 warnings | 0 notes

## Code quality

The package was also checked with `lintr`. The remaining messages are related to:

* `ggplot2` ggproto naming conventions.
* Internal object names retained for consistency throughout the package.

## Comments

This is the initial submission of **ggtwotone**.
